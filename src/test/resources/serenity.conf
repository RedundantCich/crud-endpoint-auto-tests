serenity {
  project.name = "Crude Endpoint Automated Tests"
  step.delay = 500  # I don't want to kill the service
  outputDirectory=${?serenity.outputDirectory}
  rest {
    logging=ALL
    connectionTimeout = 2500   # ms
    socketTimeout = 5000      # ms
    connectionRequestTimeout = 2000 # ms
  }
}

environments {
  dev {
    base.url = "http://77.102.250.113:17354/api/v1"
    endpoints {
      books = "/books"
      bookById = "/books/{id}"
    }
  }
}