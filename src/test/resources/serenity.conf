serenity {
  project.name = "Crude Endpoint Automated Tests"
  environment = "dev"

  environments {
    dev {
      base.url = "http://77.102.250.113:17354"
      endpoints {
        books = "/api/v1/books"
        bookById = "/api/v1/books/{id}"
      }
    }
  }
  default.environment = dev
}
